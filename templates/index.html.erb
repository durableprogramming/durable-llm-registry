<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Durable LLM Registry</title>
   <script src="https://cdn.jsdelivr.net/npm/@unocss/runtime"></script>
  <style>
    body { font-family: 'Inter', sans-serif; }
  </style>
</head>
<body class="bg-gradient-to-br from-slate-900 via-purple-900 to-slate-900 min-h-screen text-white">
  <div class="container mx-auto px-4 py-16">
    <header class="text-center mb-16">
      <h1 class="text-6xl font-bold bg-gradient-to-r from-cyan-400 to-purple-400 bg-clip-text text-transparent mb-4">
        Durable LLM Registry
      </h1>
      <p class="text-xl text-slate-300 max-w-2xl mx-auto">
        Comprehensive catalog of Large Language Models from leading providers
      </p>
    </header>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
      <% providers.each do |provider| %>
        <a href="<%= provider %>/index.html" class="group">
          <div class="bg-white/10 backdrop-blur-lg rounded-2xl p-6 border border-white/20 hover:border-cyan-400/50 transition-all duration-300 hover:scale-105 hover:shadow-2xl hover:shadow-cyan-400/20">
            <h2 class="text-2xl font-semibold text-cyan-400 group-hover:text-cyan-300 mb-2 capitalize">
              <%= provider %>
            </h2>
            <p class="text-slate-400 group-hover:text-slate-300">
              Explore <%= provider %> models and capabilities
            </p>
            <div class="mt-4 flex items-center text-cyan-400 group-hover:text-cyan-300">
              <span class="text-sm font-medium">View Catalog</span>
              <svg class="w-4 h-4 ml-2 transition-transform group-hover:translate-x-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
              </svg>
            </div>
          </div>
        </a>
      <% end %>
    </div>

    <footer class="text-center mt-16 text-slate-500">
      <p>Last updated: <%= Time.now.strftime('%Y-%m-%d %H:%M UTC') %></p>
    </footer>
  </div>
</body>
</html>